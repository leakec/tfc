[project]
requires-python = ">=3.6"

[build-system]
requires = ["setuptools>=42", 
            "wheel", 
            "numpy>=1.15",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100
target-version = ['py36','py37','py38']
