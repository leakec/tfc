[project]
requires-python = ">=3.7"

[build-system]
requires = ["setuptools>=42", 
            "wheel", 
            "numpy>=1.15",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100
target-version = ['py37','py38','py39']
