* To build from source:
python setup.py sdist bdist_wheel

* To push to PyPi Test:
python -m twine upload --repository testpypi dist/*

* To install from PyPi:
pip3 install -i https://test.pypi.org/simple/ TFC
